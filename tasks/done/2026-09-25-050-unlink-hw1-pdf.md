---
id: 2026-09-25-050
type: chore
title: Stop linking HW1's PDF from the app — the app's HW1 now differs from the printed one
priority: urgent
size: small
requires:
area: app
source: chat
created: 2026-09-25T15:30:00-07:00
status: done
after: 2026-09-25-046
branch:
merged_into:
---

## Description
Task 046 reworked HW1 in the app: lettered parts, fill-ins, the marks @ and #, P8's wording, the
schematic on each problem. The "Original PDF ↗" link on the HW1 overview
(`AssignmentOverview.tsx`, shown whenever `sourcePdf` is set) now leads to a document that says
something different. Gabriel (2026-09-25): "don't link it, invites confusion." The PDF itself
gets updated anyway (task 051), but stays unlinked.

## Done when
`hw1.json` carries no `sourcePdf`, so the overview shows no PDF link. `statementFormatCheck`
still requires every other homework to link its PDF, and names HW1 as the deliberate exception
with the reason. Gates green.

## Design
Data, not code: the link already renders only when `sourcePdf` is set. The pin keeps its teeth
for HW2–HW7 via an explicit, documented exception list.

## Verify
`statementFormatCheck`, all gates; the HW1 overview has no PDF link.

## Progress log
- 2026-09-25 — filed and claimed. Next: drop `sourcePdf`, adjust the pin, gates, land, release.
- 2026-09-25 — landed. `hw1.json` drops `sourcePdf`, so the overview shows no "Original PDF"
  link (the file stays in the repo, updated by task 051). `statementFormatCheck` keeps requiring
  the link for HW2–HW7 and names HW1 in an `UNLINKED_PDF` set, failing if HW1 links it again.
  Gates: tsc ×2, `npm run check`, build, server check — green. Code commit b4681fa.
