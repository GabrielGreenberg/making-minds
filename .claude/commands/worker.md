---
description: Unattended worker — claims ONE size:small task from tasks/incoming/, fixes it in a worktree, verifies, merges to main, logs. Foreground use is `/loop /worker`.
---

Read `tasks/PROFILE.md`, then `tasks/WORKER.md`, both in full. Process exactly one task
this run, then stop.
