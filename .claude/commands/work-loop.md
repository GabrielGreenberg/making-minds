---
description: Attended work loop — works the ready queue task by task (one Workflow per task), landing each, until nothing more can move without Gabriel. Start it as `/loop /work-loop` (args — push | release | max=N | task ids).
---

Read `tasks/PROFILE.md`, `tasks/WORK.md` and `tasks/LOOP.md`, all in full — once per session;
on a re-fire, if they are already in your context, don't re-read them. Then act as the loop
session: run one iteration (LOOP.md §2) and schedule the next, or stop and report (§6).

$ARGUMENTS
