.DS_Store
node_modules
# The root has no package.json: a lockfile here is npm run in the wrong folder (task 060)
/package-lock.json
dist
dist-ssr
*.local
.claude/

notes/

# server database files
*.sqlite
*.sqlite-wal
*.sqlite-shm

# local credentials — never committed
secrets/
ssh/
# the pilot box's shell dotfile, kept in its clone
.bashrc

# class lists (student records, FERPA) — never committed. Keep them outside
# the repo or in rosters/; *-csv.csv is the registrar's export naming.
rosters/
*-csv.csv
